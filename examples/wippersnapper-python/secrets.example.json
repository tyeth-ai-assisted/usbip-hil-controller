{
  "_comment": "Drop-in secrets file for the Python Wippersnapper client (single-board-computer variant, currently private/unreleased). Copy to secrets.json on the SBC under test; the HIL controller injects values from per-repo secrets at deploy time.",

  "io_username": "${HIL_IO_USERNAME}",
  "io_key":      "${HIL_IO_KEY}",

  "_network_comment": "On an SBC the controller usually relies on the system network stack; these fields are only honoured if the firmware is asked to bring its own.",
  "network_type_wifi": {
    "network_ssid":     "${HIL_WIFI_SSID}",
    "network_password": "${HIL_WIFI_PASSWORD}"
  },

  "protomq": {
    "host": "${HIL_PROTOMQ_HOST}",
    "port": "${HIL_PROTOMQ_PORT}",
    "tls":  false
  }
}
